<%= form_with(model: borrowing_history, local: true) do |form| %>
  <% if borrowing_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(borrowing_history.errors.count, "error") %> prohibited this borrowing_history from being saved:</h2>

      <ul>
        <% borrowing_history.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.text_field :book_id %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <div class="field">
    <%= form.label :issue_date %>
    <%= form.date_select :issue_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
